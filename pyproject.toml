[project]
name = "enterprise-brain"
version = "0.1.0"
description = "GraphRAG over markdown: Qdrant for meaning, Neo4j for structure, an agent that uses both."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agent-framework>=1.11.0",
    "gradio>=6.14.0",
    "ipykernel>=6.29.0",
    "jupyterlab>=4.3.0",
    "neo4j>=6.0.2",
    "openai>=1.68.2",
    "pydantic>=2.11.0",
    "python-dotenv>=1.2.2",
    "qdrant-client>=1.18.0",
]

[tool.uv]
package = false
